# Changelog

## [Unreleased]
### Added

- Added `@dotbot/git` with repository status (branch and porcelain changes), single-path staging and unstaging, and commits guarded against an empty message and paths outside the repository. Status reports a missing Git binary or a project that is not a repository instead of throwing.
